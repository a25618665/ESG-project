#!/usr/bin/env node

require("dotenv").config({ quiet: true });

const { createApp } = require("../app");
const { normalizePort, startServer } = require("../src/serverLifecycle");

const app = createApp();
const port = normalizePort(process.env.PORT || "3000");

app.set("port", port);
startServer({ app, port });
